server:
  port: 3344

spring:
  application:
    name:  microservicecloud-config
  cloud:
    config:
      server:
        git:
#          uri: git@github.com:zhengchaoz/microservicecloud-config.git #GitHub上面的git仓库名字，报错Auth fail
          uri: https://github.com/zhengchaoz/microservicecloud-config.git #GitHub上面的git仓库名字